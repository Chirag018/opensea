import React from 'react'

function Categories_repeator({title,para}) {
    return (
            <div>
                <h1 class="text-blue-700">{title}</h1>
                <span>{para}</span>
            </div>
    )
}

export default Categories_repeator